# 首页样式美化方案

## 目标
将首页的视觉风格升级，达到聊天页面和登录页面的视觉水准，包括：
- 渐变背景
- 装饰性光晕元素
- 毛玻璃效果增强
- 完整的深色模式适配

## 当前问题分析

### 首页现状
1. **背景单调**：使用纯色 `$color-bg-page`，缺乏层次感
2. **无装饰元素**：没有背景光晕等视觉装饰
3. **头部效果一般**：毛玻璃效果存在但不够精致
4. **深色模式不完整**：部分元素缺少深色适配

### 参考风格（聊天页 & 登录页）
1. **渐变背景**：`linear-gradient(170deg, $color-bg-gradient-start 0%, $color-bg-page 35%, $color-bg-page 100%)`
2. **装饰光晕**：使用 `radial-gradient` 创建圆形光晕，深色模式使用荧光色
3. **毛玻璃卡片**：`backdrop-filter: blur(40px) saturate(180%)`
4. **完整深色适配**：所有文字、边框、背景都有深色模式变量

---

## 修改步骤

### 步骤 1：添加渐变背景和装饰光晕

**修改 `.index-page` 样式：**
```scss
.index-page {
  min-height: 100vh;
  height: 100vh;
  background: linear-gradient(170deg, $color-bg-gradient-start 0%, $color-bg-page 35%, $color-bg-page 100%);
  position: relative;
  overflow: hidden;
  
  [data-theme="dark"] & {
    background: linear-gradient(170deg, var(--color-bg-gradient-start, #1A1520) 0%, $color-bg-page 35%, $color-bg-page 100%);
  }
}
```

**添加装饰光晕元素（在模板中）：**
```html
<view class="bg-decoration">
  <view class="decoration-circle circle-1"></view>
  <view class="decoration-circle circle-2"></view>
</view>
```

**添加装饰光晕样式：**
```scss
.bg-decoration {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  pointer-events: none;
  overflow: hidden;
  
  .decoration-circle {
    position: absolute;
    border-radius: 50%;
    opacity: 0.5;
  }
  
  .circle-1 {
    width: 400rpx;
    height: 400rpx;
    top: 200rpx;
    right: -100rpx;
    background: radial-gradient(circle, rgba($color-primary, 0.15) 0%, transparent 70%);
    
    [data-theme="dark"] & {
      background: radial-gradient(circle, rgba(217, 70, 239, 0.2) 0%, transparent 70%);
    }
  }
  
  .circle-2 {
    width: 300rpx;
    height: 300rpx;
    bottom: 300rpx;
    left: -80rpx;
    background: radial-gradient(circle, rgba($color-accent, 0.1) 0%, transparent 70%);
    
    [data-theme="dark"] & {
      background: radial-gradient(circle, rgba(0, 245, 212, 0.12) 0%, transparent 70%);
    }
  }
}
```

### 步骤 2：增强头部毛玻璃效果

**修改 `.fixed-header` 样式：**
```scss
.fixed-header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  background: $color-bg-card;
  backdrop-filter: blur(40px) saturate(180%);
  -webkit-backdrop-filter: blur(40px) saturate(180%);
  padding: $space-md $space-md $space-sm;
  border-bottom: 1px solid $color-border;
  
  [data-theme="dark"] & {
    background: var(--glass-card-bg, rgba(255, 255, 255, 0.06));
    border-bottom: 1px solid var(--glass-border, rgba(255, 255, 255, 0.12));
  }
}
```

### 步骤 3：优化内容区域层级

**修改 `.page-content` 样式：**
```scss
.page-content {
  padding-left: 0;
  padding-right: 0;
  padding-bottom: 0;
  min-height: 100vh;
  position: relative;
  z-index: 1;
}
```

### 步骤 4：优化轮播图区域

**修改 `.swiper` 样式：**
```scss
.swiper {
  padding: $space-xl $space-md 0 $space-md;
  box-sizing: border-box;
  
  :deep(.ui-swiper) {
    border-radius: $radius-xl;
    overflow: hidden;
    box-shadow: $shadow-md;
    
    [data-theme="dark"] & {
      box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.3);
    }
  }
}
```

### 步骤 5：优化商品列表区域

**修改 `.goods-list` 样式：**
```scss
.goods-list {
  padding: $space-md;
  box-sizing: border-box;
  overflow: hidden;
  
  :deep(.ui-goods-card) {
    background: $color-bg-card;
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid $color-border-light;
    border-radius: $radius-lg;
    overflow: hidden;
    
    [data-theme="dark"] & {
      background: var(--glass-card-bg, rgba(255, 255, 255, 0.06));
      border: 1px solid var(--glass-border, rgba(255, 255, 255, 0.12));
    }
  }
}
```

### 步骤 6：优化加载更多区域

**修改 `.load-more` 样式：**
```scss
.load-more {
  padding: $space-md 0;
  
  :deep(.ui-divider) {
    color: $color-text-sub;
    
    [data-theme="dark"] & {
      color: var(--color-text-sub, #A1A1AA);
    }
  }
}
```

---

## 需要检查的 UI 组件

在修改首页后，需要确保以下组件也有正确的深色模式适配：

1. **UiSearch** - 搜索框组件
2. **UiTabs** - 分类标签组件
3. **UiSwiper** - 轮播图组件
4. **UiWaterfalls** - 瀑布流布局组件
5. **UiGoodsCard** - 商品卡片组件
6. **UiDivider** - 分割线组件
7. **TheTabbar** - 底部导航栏

---

## 预期效果

### 浅色模式
- 温暖的渐变背景，从暖色调过渡到主背景色
- 橙色系装饰光晕，增添活力感
- 毛玻璃效果的头部和卡片，通透有质感
- 整体视觉层次分明

### 深色模式
- 深邃的紫黑渐变背景
- 荧光紫/青色装饰光晕，赛博朋克风格
- 幽灵玻璃效果的头部和卡片
- 所有文字清晰可读，对比度适中

---

## 实施顺序

1. 修改首页模板，添加装饰元素
2. 修改首页样式，实现渐变背景和光晕
3. 增强头部毛玻璃效果
4. 优化各区域的深色模式适配
5. 检查并修复相关 UI 组件的深色模式
6. 测试验证效果
